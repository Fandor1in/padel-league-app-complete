// Utility functions for calculating player and pair ratings
export const calculatePlayerRating = (wins: number, losses: number): number => {
  return wins * 3 + losses * 0;
};

export const calculatePairRating = (wins: number, losses: number): number => {
  return wins * 3 + losses * 0;
};