import React from 'react';

const AddMatch: React.FC = () => {
  // Placeholder component for adding a match result
  return (
    <div>
      <h1>Add Match Result</h1>
      <p>Submit the result of a match for confirmation by your opponents.</p>
    </div>
  );
};

export default AddMatch;